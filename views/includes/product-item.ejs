<section class="product-item">
    <img src="<%=product.imageUrl%>" alt="<%=product.title%>">
    <div class="product-item-content">
        <h2>
            <%=product.title%>
        </h2>
        <div class="product-item-actions">
            <% if(locals.isAdmin){ %>
                <a href="/admin/products/<%=product.id%>" class="btn btn-alt">View & Edit</a>
                
                <button class="btn btn-alt" data-productid="<%= product.id %>"
                    data-csrf="<%= locals.csrfToken %>">Delete</button>
                <% } %>
        </div>
    </div>
</section>

<!-- data-productId="<%=product.id%>" data-csrf="<%=locals.csrfToken%>" -->

<!-- data-productid="<%= product.id %>" data-csrf="<%= locals.csrfToken %>" -->